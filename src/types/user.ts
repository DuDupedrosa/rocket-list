export type UserLocal = {
  name: string;
  lastName: string | null;
  email: string;
  id: string;
};

export type UserProfile = {
  id: string;
  email: string;
  name: string;
  lastName: string | null;
};
